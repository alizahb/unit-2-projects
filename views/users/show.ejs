<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageOwner.username %>'s Inventory</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Welcome to <%= pageOwner.username %>'s Inventory</h1>
  <% if(pageOwner.inventory.length) { %>
  <p>Currently stocked with:</p>
  <ul>
    <% pageOwner.inventory.forEach((item)=>{ %>
      <li><%= item.name %></li>
    <% }) %>
  </ul>
  <% } else { %>
    <p>There are no items in this inventory.</p>
  <% } %>
</body>
</html>